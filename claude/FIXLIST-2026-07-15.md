# Fix train v561 — from the 2026-07-15 morning audit

Status: ✅ = fixed in v561 (on staging/sandbox) · ⬜ = still open · 🅿 = parked, needs Spencer's call

## Shipped in v561 (pushed to staging)

1. ✅ **PRIORITY — Shopify checkout rates now flow through Admin → Rates.**
   The carrier callback (`shopify-rates.js`) loads `rateRules` + the merchant's customer
   record and prices every checkout option as: raw FedEx cost → the merchant's SELL price
   (profile service rules, surcharge rules, account markup — the exact engine the portal
   uses) → the store's Buyer markup % → handling. Admin-blocked services no longer appear
   at checkout. The Checkout Rates preview in Settings uses the same math, and the panel
   copy says so. Accounts with no rules/markup price exactly as before.
2. ✅ Cross-tab sign-out white screen (React hooks order in the app shell).
3. ✅ Assistant chat Send button (was sending "[object Object]").
4. ✅ Orders keep their **country** when opened on the Ship tab / via Scan (international
   orders no longer quote as domestic).
5. ✅ New customers and new logins carry `createdAt`, re-arming the server guard that
   protects fresh rows from stale-tab overwrites (the July-13 incident class).
6. ✅ Rule editor: "Route to Printer" has a printer-name field now; switching action type
   no longer leaks the previous value; dead "Add Starter Rules" button replaced with
   "＋ Build Your First Rule".
7. ✅ "Send reset email" (Users admin) now says when no email was actually sent.
8. ✅ Returns "Reason" default matches its dropdown options.
9. ✅ Branding public-page toggle merges instead of wiping `publicBrand`.
10. ✅ Company address deploy excludes disabled logins (matched the server's status string).
11. ✅ Batch zone chips/sort no longer show "ZNaN" when the customer record has no origin
    ZIP but a sender ZIP is set.
12. ✅ New Order: empty form now explains itself instead of silently doing nothing.
13. ✅ Address Book CSV import uses the proper RFC-4180 parser (quoted commas —
    "Acme, Inc." — no longer garble columns).
14. ✅ Checkout Rates "registered" badge reflects the last real status check (was
    always-green even with no Shopify connected).
15. ✅ Ship-screen notify card: both emails carry the real tracking number, buttons stay
    disabled until a label is booked, caption tells the truth; the duplicate-label banner's
    Reprint pulls the actual PDF from the label store.

## Still open (from the audit) — next batch

16. ⬜ Rates multi-tab draft clobber: two dirty Rates screens can silently revert each
    other's saves. Needs a dirty-draft warning or per-section draft scoping. **Until then:
    edit + save one Rates screen at a time.**
17. ⬜ Admin "Integration (admin)" API key creation always rejected server-side
    (db.js requires a clientId; the admin-key flow is supposed to be customer-less).
18. ⬜ Admin Billing: can issue invoices from a partially-loaded snapshot (no warning), and
    matches shipments by customer NAME so renames orphan history. Guard + id-matching.
19. ⬜ Shipments "Check for labels" can never resolve (server status action is hard-coded
    "nothing pending").
20. ⬜ Public API gaps vs portal: built-in 2025 list book (stubbed server-side), Saturday
    fee rule ignored, imported One Rate tables not returned.
21. ⬜ Demo-mode assistant always answers "Sign in to use the assistant."
22. ⬜ Local-mode "Request account" stores nothing (stub with a success message).
23. ⬜ Rules "apply without booking" flow exists but no button reaches it.
24. ⬜ Package Sizes: "Empty (lb)" shown but not settable; boxes not editable.
25. ⬜ Duplicate-email login creation silently keeps the old password.
26. ⬜ Dead code cleanup: `src/quote.js` (stale, auth-less copy of the deployed quote
    function — delete), OrderDetail component, unreachable Platform-accounts panel.

## Parked — needs Spencer's decision 🅿

27. 🅿 **Branding / Domains persistence**: both save only to the signed-in admin's own
    settings. If white-labeling is meant to be seen by anyone else, this needs a shared
    store + real tenant registration — design decision, not a patch.
28. 🅿 **Billing "Add funds"** credits prepaid balance with no payment step — needs either
    a disclaimer or a real payment flow.
29. 🅿 Checkout buyer-markup default is 20% — now that it rides on the SELL price, existing
    stores' buyer prices went UP by their account markup at their next checkout quote.
    If any live store has checkout rates on, decide whether their Buyer markup % should
    be trimmed. (Token-connect stores with no admin markup are unaffected.)

Deploy state: staging (sandbox) = v561. Production (main) untouched — Spencer's word
required before promoting.
