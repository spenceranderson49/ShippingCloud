# Handoff — ShippingCloud / Freightwire (2026-07-14, end of the marathon session)

Owner: **Spencer Anderson** (spenceranderson49@gmail.com) — non-technical, rapid-fire, sends screenshots,
(rightly) furious about data loss. Lead with data safety, preview visual redesigns before shipping.
Repo: `spenceranderson49/ShippingCloud`.

## Branch / deploy state
- **Production (`main`) = staging = feature branch `claude/shopify-launch-db-recovery-v1tdya` = addr-v549.**
  Everything v509–v549 is LIVE on shippingcloud.net + freightwireship.com. Tests: `bash claude/tests/run.sh`.
- Deploy flow: feature branch → `staging` (shippingcloud-staging.netlify.app) → `main`. Bump `BUILD_TAG`
  in src/App.jsx each release. Spencer re-authorized production pushes mid-session; if a new session
  starts, CONFIRM before touching production — his default stance is staging-first.
- Older context: `claude/HANDOFF-2026-07-13-session2.md` (DB layout, Supabase access, safety protocol).

## Safety protocol (unchanged — read it)
Additive/surgical DB writes only; snapshot to `bak:<key>:<iso>` before EVERY write; ask before deletes;
never put model IDs in commits/code. Supabase creds come from Netlify env on prod site
`65e219f6-c528-44cc-8134-3953a1bfa1b5` (`SUPABASE_URL`, `SUPABASE_SERVICE_KEY`), table
`app_stores(tenant,key,value)`. Tenants: `main` = production, `sandbox` = old source DB.

## What happened on 2026-07-13 → 07-14 (data)
1. **Stale-tab wipes (root cause #1):** tabs opened before a restore wrote whole stale copies back on
   save. Wiped `users`+`clients` (Granite Seed `u1783735956593984` + Riley Blake `u1783917884617639`)
   at 17:27Z and 21:35Z, and `featureFlags` twice (~03:25Z, 03:33Z). All restored from bak rows and
   verified. **v526 added server guards in netlify/functions/db.js**: a users/clients write missing ≥2
   existing rows re-adds them (single deletes still work); featureFlags saves merge per-login.
2. **7/11 migration drift (root cause #2):** restored additively for LAgence (`u1783446563465`):
   4 real Autopilot rules, printer config (ZPL/"Jaden"/4x675/slip-off + printers/printRoutes/receipt/
   defaultPrinterId), real box sizes (XS 10×10×3, S 17×10×3, M 18×11×5, L 20×15×5), and 51 missing
   shipment records (main now 196 — fixed printed orders lingering on the Ship picker).
3. **byoCarrier flag** ON for LAgence login (Carrier-accounts tab is feature-gated since v465; admin
   controls it under Features & Access). Wiped once by a stale tab, restored; now guard-protected.
4. Full audit vs the 08:01Z `bak:full` + sandbox: nothing from that morning is missing.

## OPEN items (need Spencer's word)
- **LAgence history merge from sandbox:** orders 357→656, **ledger 7→194 (billing history — important)**,
  emails 7→149, audit 7→194, manifests 0→1. Additive by id, snapshot first. Asked repeatedly; no answer.
- **Riley Blake**: 7 demo rules (5 factory `rs*` + 2 made in-app early July). Real or test? Clear?
- **LAGENCE client markup**: sandbox copy says 15%; pre-overwrite main said 0%. Which is right?
- Orphan `u1fw` data in sandbox (19 keys, no user entry anywhere) — flagged, left alone.
- **Shopify public app**: code done (`claude/SHOPIFY-PUBLIC-APP.md`); remaining = Spencer's Partner
  Dashboard clicks + listing content + submit (1–3 week review).
- Build menu he hasn't picked from: Cmd+K quick-switcher, CSV address import, savings report, branded
  tracking page, onboarding checklist, weekly report email, duplicate-address cleanup.

## Release notes v509–v549 (all in production)
- **v509–v514** (previous session): admin login-row overflow menu; byo-carrier section removal;
  assistant-button cleanup; **guardian nightly drift alarm (active — emails Spencer)**; clean new
  accounts; "Freightwire AI" rename; company shared address book; companySetFlags boolean-flatten fix.
- **v515–v516**: full-bleed cloud tab icon + follows accent; accent-following logo; surface/app/login
  backgrounds REMOVED (accent stays); dark & grey themes REMOVED; Department field in order popup
  (flows to FedEx DEPARTMENT_NUMBER); popup's duplicate Residential checkbox removed; pinned-Save-bar
  fix (--sc-stickoff CSS var; bar was hiding under the sticky header); "already has a label" banner
  re-arms on order change; FedEx wordmark off the doc-tab preview; booking popup de-crammed.
- **v517**: Print settings gets click-to-Save via `useSettingsDraft` (buffers the section's own patches —
  NOT an object copy, so company-deployed data can't bake into personal settings); terms §6 removed.
- **v518**: One Rate ranks under its base service; rate-list resetKey excludes the One Rate box (fixed
  Autopilot flashing); "Package details" rename.
- **v519–v521**: step-header + hide-assistant toggles; saner order sorts; Today-button style; matched
  fonts/widths in the package strip; six "Ask AI to…" buttons removed; assistant fab has wrapped name +
  session-dismiss X; **Quick quote opens blank every time**; **Ship keeps work-in-progress**
  (localStorage `shipWip`); swap no longer trips empty-sender nags (swapMuted).
- **v522–v543**: orders-rail redesign (approved via previews): 2×3 identical centered boxes — Search+All
  stores / Newest+Today / date–date (floated dash), custom select chevrons, refresh beside the count,
  identical fonts/weights. Steps-off Ship layout (approved geometry — TOUCH ONLY IF ASKED): cards flush
  with ORDERS title (`lg:!mt-0` beats space-y from the display:none mobile row), actions float
  `absolute -top-7 right-0` sized `lg:w-[250px] xl:w-[290px]` to mirror the scan box, wrapper `pt-2.5`.
- **v544** (overnight batch): **Rule Tester** on Autopilot (per-condition pass/fail vs actual order
  values); **Yesterday digest** strip on Dashboard; **individual-LOGIN restore** in Admin → Backups
  (same picker customers had); **1-2-3 step headers hidden by default** (toggle inverted to "Show…");
  "total X lb" mono label fixed; Insure $/Signature sized to match the strip.
- **v545–v547**: consistency sweep — one focus-border blue (#0099FF), one input radius (rounded-lg),
  order-popup save button standard blue; then **font-mono stripped from ALL app UI (175 uses)** incl.
  Autopilot/Batch date inputs, tracking numbers, money columns, and the shared Settings `Input`
  component. Mono remains ONLY in CSV/JSON paste boxes, printed-paper templates, and the label mock.
- **v548–v549**: Autopilot tagline removed; Batch's Saved-batches bar moved below the filters card.

## Watch-outs
- Spencer keeps very old tabs open across Chrome AND Edge — before any DB restore, have him hard-refresh
  every ShippingCloud/Freightwire tab, or a stale tab may write old data back (guards cover only
  users/clients/featureFlags).
- The guardian nightly (v512) emails drift alerts — expect Spencer to forward flags from its first runs.
- UI work: he wants pixel alignment, ONE font (Inter), no taglines/clutter, and **previews first** for
  redesigns (SendUserFile a mock screenshot; he answers fast). Small nudges can ship directly.
- Do NOT reintroduce: dark/grey themes, surface/background customizations, "Ask AI to…" buttons,
  Autopilot header's auto-run pill + Apply-only button (auto-run now lives in Print settings →
  Ship & print automation), Filter & batch's "Ship with" override, font-mono in UI.
- The Ship-screen steps-off geometry and the orders-rail 2×3 grid are Spencer-approved layouts — don't
  refactor them casually; he notices single pixels.
