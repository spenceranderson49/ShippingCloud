# Handoff — ShippingCloud / Freightwire (2026-07-14, session close at addr-v558)

Owner: **Spencer Anderson** (spenceranderson49@gmail.com) — non-technical, rapid-fire, screenshots.
Lead with data safety; preview visual redesigns before shipping. Repo: `spenceranderson49/ShippingCloud`.

---

## ⭐ TODAY'S PRIORITY: Shopify PUBLIC app submission (Spencer is doing this TODAY)

Full playbook with every detail: **`claude/SHOPIFY-PUBLIC-APP.md`**. The code side is 100% done and
live (OAuth install/callback with HMAC, GDPR compliance webhooks, minimal scopes, multi-store,
auto-detection of OAuth vs token flow). What remains is Spencer's clicks:

1. **partners.shopify.com** → create free Partner account → **Apps → Create app manually**, name it
   to match the brand (ShippingCloud).
2. **Configuration**: App URL `https://shippingcloud.net/`; Allowed redirection URLs (one per line):
   - `https://shippingcloud.net/.netlify/functions/shopify-auth`
   - `https://www.shippingcloud.net/.netlify/functions/shopify-auth`
   - `https://freightwireship.com/.netlify/functions/shopify-auth`
   - `https://www.freightwireship.com/.netlify/functions/shopify-auth`
   - `https://shiphub-staging.netlify.app/.netlify/functions/shopify-auth`
   **Embedded app: OFF** (standalone — no App Bridge needed).
3. Copy the **Client ID + Client secret** into Netlify env on EACH site (prod ×2, staging):
   `SHOPIFY_API_KEY`, `SHOPIFY_API_SECRET`, `APP_URL` (that site's own URL) → **redeploy each site**.
   (These were already set on production at last check — verify, then just add staging if missing.)
4. **Scopes** (Partner Dashboard → API access) — EXACTLY these, they match the code:
   `read_orders, read_products, write_shipping, read_fulfillments, write_fulfillments,
   read_assigned_fulfillment_orders, write_assigned_fulfillment_orders,
   read_merchant_managed_fulfillment_orders, write_merchant_managed_fulfillment_orders`
   (Recommended: submit WITHOUT `write_orders`; the address push-back button then only works for
   token-connected stores. Add the scope in a later release.)
5. **Protected customer data** declaration: purpose = order fulfillment / shipping labels ("App
   functionality"); request name, address, email, phone; retention = merchant's own account store,
   purged on `shop/redact` (endpoint live); security = TLS + Supabase at rest.
6. **Compliance webhooks** — all three topics to: `https://shippingcloud.net/.netlify/functions/shopify-compliance`
   (already returns 401 on bad HMAC / 200 on good — passes Shopify's automated check).
7. **Listing**: name ShippingCloud; tagline "Discounted FedEx labels, live rates, and hands-free order
   fulfillment."; description + feature bullets drafted in the playbook; **assets Spencer must make**:
   1200×1200 icon, 3–6 screenshots at 1600×900 (Ship screen w/ rates, Orders, Autopilot, Quick Quote,
   booked label). Pricing: **Free** (billed directly — keeps review simple). Privacy policy page is
   live at `/privacy.html`.
8. **Pre-submission test** on a free dev store: install via the app link → consent → land back
   connected; sync orders; book+void a test label; uninstall/reinstall clean. Provide the reviewer a
   sandbox demo login + steps in the listing form.
9. **Submit.** Typical first response 5–10 business days, total 1–3 weeks. The token-connect flow
   keeps working for any store in the meantime — the public app is the long-term fix, not a blocker.

If Shopify review sends feedback, any code fixes are on us — paste the review email into a session.

---

## Branch / deploy state
- **Production (`main`) = staging = feature branch `claude/shopify-launch-db-recovery-v1tdya` = addr-v558.**
  Live on shippingcloud.net + freightwireship.com. Tests: `bash claude/tests/run.sh` (ALL PASS).
- Deploy flow: feature branch → `staging` → `main`; bump `BUILD_TAG` in src/App.jsx per release.
  Spencer authorized production pushes late in this session, but a NEW session should confirm first.

## Safety protocol (unchanged)
Additive/surgical DB writes only; snapshot to `bak:<key>:<iso>` before EVERY write; ask before deletes;
no model IDs in commits/code. Supabase creds from Netlify env on prod site
`65e219f6-c528-44cc-8134-3953a1bfa1b5`; table `app_stores(tenant,key,value)`; tenants `main` (prod) /
`sandbox` (old source DB).

## Data events this session (all resolved, all snapshotted)
1. **Stale-tab wipes**: old tabs rewrote whole stores on save — erased Granite Seed + Riley Blake
   logins twice (users/clients @17:27Z, 21:35Z) and featureFlags twice (~03:25Z, 03:33Z). Restored
   each time. **v526 server guards** (db.js): users/clients writes missing ≥2 existing rows get them
   re-added; featureFlags merges per-login.
2. **Migration drift** restored additively for LAgence `u1783446563465`: 4 real rules, printer config
   (ZPL/"Jaden"/4x675), real box sizes, 51 shipment records (→196), byoCarrier flag.
3. **Default sender wiped 3×** by the Ship form's sender→settings auto-save (via the swap button).
   Value (restored, verified): Spencer Anderson · Freightwire · 277 West 960 South, American Fork UT
   84003 · 8018825247 · spencer@freightwire.com. **v554 REMOVED that auto-save entirely** — the ship
   form has no write path to the default sender; it changes only in Settings → General or explicitly.
   The sender address-book dropdown now has a pinned "Default sender" restore row.

## OPEN items (need Spencer's word)
- **LAgence history merge from sandbox**: orders 357→656, **ledger 7→194 (billing history!)**, emails
  7→149, audit 7→194, manifests 0→1. Additive by id, snapshot first. Asked several times, no answer.
- **Riley Blake** (`u1783917884617639`): 7 demo rules — real account or test? Clear?
- **LAGENCE client markup**: 15% (sandbox copy, current) vs 0% (pre-overwrite). Which is right?
- Orphan `u1fw` data in sandbox — flagged, untouched.
- Unpicked build menu: Cmd+K switcher, CSV address import, savings report, branded tracking page,
  onboarding checklist, weekly report email, duplicate-address cleanup.

## Release highlights v509–v558 (details in git log; all live)
- Data/infra: guardian nightly drift alarm (v512, active); stale-tab write guards (v526); individual
  LOGIN restore picker in Admin → Backups (v544); sender auto-save removed (v554).
- Features: Rule Tester on Autopilot (v544); Yesterday digest on Dashboard (v544); Quick Quote opens
  blank (v522); Ship keeps work-in-progress across tabs (`shipWip`, v522); Department field on order
  popup (v516); pinned click-to-Save DraftBar on Print settings (v517) + General, Warehouses, Package
  Sizes, Box Logic, Reference Fields, Email Automation, Commercial Invoice, Other Documents, Checkout
  Rates (v554) via `SettingsDraftWrap` (buffered patches — never bakes company-deployed data in).
- Look & feel (Spencer-approved; don't refactor casually): full-bleed accent-following cloud tab icon;
  dark/grey themes + surface/bg customizations REMOVED (accent stays); orders-rail 2×3 identical
  centered boxes; steps-off Ship geometry (cards flush with ORDERS title, floating actions
  `-top-7 right-0` w-[250/290px], wrapper pt-2.5, `lg:!mt-0` on the grid — load-bearing); package
  strip rows mirror each other (Signature under Ship Date, Insure under Ref #); ONE font everywhere
  (175 font-mono uses stripped; mono only in paste boxes/printed templates/label mock); one focus
  blue, one input radius, one toolbar button height (px-3 py-1.5; w-full CTAs py-2), matched
  address-check chips (w-32), one modal title style, Title Case EVERYWHERE (v556–v558: nav, buttons, options, placeholders, expanders, spans — 270+ strings over three rounds; verified against the live production bundle). CONVENTION: labels/buttons/options/placeholders are Title Case (minor words a/an/the/of stay lowercase mid-phrase); hints, descriptions and sentences stay sentence case; units (lb/oz) and formats (INV-…, mm/dd/yyyy) untouched. New UI text must follow this.
- Removed on request (do NOT reintroduce): taglines under tab headers, "Ask AI to…" buttons,
  Autopilot auto-run pill + Apply-only button (auto-run lives in Print settings → Ship & print
  automation), Filter & Batch "Ship with" override, "Create shipment"/"Select service" titles,
  1-2-3 steps ON by default (now hidden by default).

## Watch-outs
- Old tabs (Chrome AND Edge) = data risk beyond the guarded stores. Hard-refresh before DB work.
- Nag philosophy: errors only when the user ACTS (booking-time validation), not standing banners.
- Labels are Title Case; hints/descriptions stay sentence case.
- He notices single pixels. Preview-first for anything visual that's bigger than a nudge.
