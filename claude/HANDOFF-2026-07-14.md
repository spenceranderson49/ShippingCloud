# Handoff — ShippingCloud / Freightwire (2026-07-14, after the marathon session)

Owner: **Spencer Anderson** (spenceranderson49@gmail.com) — non-technical, rapid-fire, sends screenshots,
(rightly) furious about data loss. Lead with data safety. Repo: `spenceranderson49/ShippingCloud`.

## Branch / deploy state
- **Production (`main`) = staging = feature branch `claude/shopify-launch-db-recovery-v1tdya` = addr-v542.**
  Everything v509–v542 is LIVE on shippingcloud.net + freightwireship.com. Tests: `bash claude/tests/run.sh`.
- Deploy flow unchanged: feature branch → `staging` → `main`; bump `BUILD_TAG` in src/App.jsx each release.
- Older context: `claude/HANDOFF-2026-07-13-session2.md` (DB layout, Supabase access, safety protocol).

## Safety protocol (unchanged — read it)
Additive/surgical DB writes only; snapshot to `bak:<key>:<iso>` before EVERY write; ask before deletes;
never put model IDs in commits/code. Supabase creds come from Netlify env on prod site
`65e219f6-c528-44cc-8134-3953a1bfa1b5` (`SUPABASE_URL`, `SUPABASE_SERVICE_KEY`), table `app_stores(tenant,key,value)`.
Tenants: `main` = production, `sandbox` = old source DB.

## What happened today (2026-07-13 → early 07-14 UTC)
1. **Stale-tab wipes (root cause of the day's data loss):** browser tabs opened before a restore write
   whole stale copies back on save. This wiped `users`+`clients` (Granite Seed + Riley Blake logins) at
   17:27Z and again 21:35Z, and `featureFlags` twice (~03:25Z, 03:33Z). All restored from bak rows each
   time (verified). **v526 added server-side guards in netlify/functions/db.js**: a users/clients write
   missing ≥2 existing rows gets them re-added (single deletes still work); featureFlags saves merge
   per-login. myAccess is NOT guarded (trivial value).
2. **7/11 migration drift (second root cause):** sandbox→prod migration dropped per-login data. Restored
   additively for LAgence (`u1783446563465`): 4 real Autopilot rules (verified), logo (was already ok),
   printer config (ZPL/"Jaden"/4x675/packing-slip off + printers/printRoutes/receipt/defaultPrinterId),
   real box sizes (XS 10×10×3, S 17×10×3, M 18×11×5, L 20×15×5), and 51 missing shipment records
   (main now 196 — this fixed printed orders lingering on the Ship picker + the yellow dup banner).
3. **byoCarrier**: Carrier-accounts tab is feature-gated per login since v465. Flag now ON for LAgence
   (wiped once by a stale tab, restored, now protected by the merge guard).
4. Full drift audit vs the 08:01Z `bak:full` + sandbox: NOTHING from the morning is missing.

## Open items (get Spencer's word before acting)
- **LAgence history merge from sandbox (pending his OK):** orders 357→656 (~299 old), ledger 7→194
  (billing history — important), emails 7→149, audit 7→194, manifests 0→1. Additive by id, snapshot first.
- **Riley Blake** (`u1783917884617639`, rileyblake@testtest.com): 7 demo rules (5 factory `rs*` +
  2 made in-app in early July). Asked repeatedly if real/test + whether to clear — no answer yet.
- **LAGENCE client markup**: sandbox copy set it to 15%; pre-overwrite main had 0%. Asked — no answer.
- Orphan `u1fw` data in sandbox (19 keys, no user entry anywhere) — flagged, left alone.
- **Shopify public app**: everything code-side done (see `claude/SHOPIFY-PUBLIC-APP.md`); remaining steps
  are Spencer's Partner-Dashboard clicks + listing content + submit (1–3 week review).
- Offered but not requested: three-column Ship layout mock (equal sender/receiver + tools panel).

## Release notes v509–v542 (all live)
- v509–v514: previous session (admin login rows, byo-carrier section removal, assistant button cleanup,
  guardian nightly drift alarm — NOW ACTIVE on prod, clean new accounts, shared address book).
- v515–v516: tab icon = full-bleed brand cloud, follows accent; accent-following logo; surface/app/login
  background customizations removed (accent kept); dark & grey themes removed; Department field in order
  popup (flows to FedEx DEPARTMENT_NUMBER); duplicate Residential checkbox removed there; pinned Save bar
  fix (was hidden under sticky header); "already has a label" banner re-arms when picking another order;
  FedEx wordmark removed from the doc-tab preview; review-before-booking popup de-crammed.
- v517: Print settings got the click-to-Save DraftBar via `useSettingsDraft` (buffers the section's own
  patches — deliberately NOT an object copy, so company-deployed data can't bake into personal settings);
  terms "Markup and reseller pricing" section removed.
- v518: One Rate rows rank under their base service; rate-list resetKey no longer includes the One Rate
  box (was causing the Autopilot open/close flashing); step-2 renamed "Package details".
- v519–v520: step-header (1-2-3) toggle + hide-assistant toggle in Ship-screen settings; saner order
  sorts; Today button inactive style; matched fonts/widths across the package strip; six "Ask AI to…"
  buttons removed; assistant fab wraps its name + session-dismiss X; Quick quote CHANGED to open blank
  every time; Ship tab keeps work-in-progress across tab switches (`shipWip` in localStorage).
- v521: sender/receiver swap no longer trips empty-sender nags (swapMuted); swap arrow styling.
- v522–v542: orders-rail redesign (Spencer-approved previews: 2×3 identical centered boxes — Search+All
  stores / Newest+Today / date–date with floated dash, custom select chevrons, refresh beside the count)
  and many steps-off Ship layout alignment rounds. FINAL layout (approved): cards flush with the ORDERS
  title (`lg:!mt-0` beats the space-y from the display:none mobile row — don't remove that), actions
  float `absolute -top-7 right-0` sized `lg:w-[250px] xl:w-[290px]` to mirror the scan box, page wrapper
  has `pt-2.5`. Spencer signed off on this exact geometry — tweak only if he asks.

## Watch-outs for the next session
- Spencer edits the DB via console AND keeps very old tabs open — before any DB restore, tell him to
  hard-refresh every ShippingCloud/Freightwire tab (Chrome AND Edge), or the app may write stale copies
  back (guards catch users/clients/featureFlags but nothing else).
- The guardian nightly (v512) now emails drift alerts — first run since deploy lands tonight; expect
  Spencer to forward whatever it flags.
- Any Ship-screen geometry change: he wants pixel alignment and previews FIRST for redesigns
  (SendUserFile a mock screenshot; he answers fast).
