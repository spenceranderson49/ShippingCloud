# Handoff — 2026-07-15 (Shopify launch day, v559)

Owner: Spencer Anderson (spenceranderson49@gmail.com) — non-technical, rapid-fire, screenshots.
Preview-first for visuals; lead with data safety. branch = staging = main = **fb36785 (v559)**.

## CLOSED by Spencer today — DO NOT RE-RAISE
- LAgence history merge from sandbox (ledger/orders/emails) — **done with it, drop it.**
- Riley Blake 7 demo rules — **done with it, drop it.**
- LAgence markup 15% vs 0% — **done with it, drop it.**

## Shopify public app (the big push — SUBMISSION IN FLIGHT)
- App "ShippingCloud" created in the NEW dev dashboard (client_id 1ffaf48bb94f9fc56e2ba6a56f327fc7).
- Config released: 5 redirect URLs, 9 minimal scopes (no write_orders), embedded off, legacy off.
- Compliance webhooks could NOT be set in the dashboard UI — deployed via Shopify CLI with
  **shopify.app.toml now versioned at repo root** (version "shippingcloud-3+"). Future config
  changes: edit that file, `shopify app deploy`.
- Assets made & delivered: real-brand icon (cloud + Space Grotesk wordmark, 1200×1200) and five
  1600×900 demo screenshots (demo banner scrubbed). Listing copy drafted in claude/SHOPIFY-PUBLIC-APP.md
  and in-chat.
- REMAINING when session closed: fix protected-customer-data declaration (was wrongly set to
  "doesn't need access" — must declare name/address/email/phone), finish listing form (incl.
  reviewer demo login!), capabilities selector, self-review checkbox, dev-store install test
  confirmation, **Submit for review**. Netlify env sanity-check (API key/secret = this app).
- Two-brands question answered: one app only for now; FreightwireShip second app possible
  post-approval (duplicate-app policy risk noted). Token-connect stores unaffected by all of this.

## v559 shipped today (prod = staging)
- 2FA: Trust This Device 30/60/90 (hashed server tokens, max 10, cleared when last factor off;
  stripUsers/mergeUsersForWrite handle trustedDevices), QR code TOTP setup (qrcode-generator dep),
  in-app Login now handles email codes too.
- Ship screen rebuilt with Spencer approving each step: Cheapest view truly price-sorted; list
  capped 52rem left-aligned; breakdown = compact table under service name (indent mirrors the
  Autopilot chip via invisible twin); service name semibold, transit line 12.5px black; right
  info column (flex-fill) = This Shipment status card + Billing + Send Label & Notify + form
  actions; Hide All Services collapses one-box mode; placeholder box before input; every column
  piece has a Hide toggle (hideFromOrderBox, hideBillingBox new).
- Look: background = **Ice #e8f2fa** (tried Cool Slate, Big Clouds sky — Ice won); stronger
  section outlines (#cbd5e1 + shadow-sm); Ship Date/Signature w-36 to match other fields.
- Settings: Activity Log panel removed from General; all Ship-screen toggles say "Hide X".
- Brand: "by Freightwire" strip removed everywhere on ShippingCloud (code-level, FW_LOGO excluded;
  custom partner logos still render); "Take a peek"/demo doors removed from shippingcloud.net
  landing (FW site keeps its link; window.__scEnterDemo does NOT exist in shipped code — a temp
  local hook was used for screenshots only); printed rate sheets use full ShippingHub lockup on FW.
- Preview pages delivered (not applied): logo-everywhere preview, 30 background options across
  3 rounds, 10 section-outline treatments (orders-side-tab idea in #5/#10 — Spencer liked it,
  unbuilt).

## Watch-outs / notes
- Session env once silently lost a local commit (Cool Slate) — if a push is rejected, fetch and
  rebase, never force.
- New dev dashboard truth: compliance webhooks + full config = CLI/TOML only.
- Ideas parked with Spencer's interest: This Shipment card extras (order items, billed/dim
  weight, address-check status, cutoff countdown, customer history); orders rail as side tab.
