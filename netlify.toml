# SPA fallback — serve index.html for all app routes.
# (Function paths under /.netlify/functions/ are routed natively by Netlify
#  before redirects run, so no rule is needed for them.)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
